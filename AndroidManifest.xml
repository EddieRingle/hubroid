<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="net.idlesoft.android.apps.github"
      android:versionCode="2012022500"
      android:versionName="3.0-alpha0">
    <uses-sdk android:minSdkVersion="7" android:maxSdkVersion="15" android:targetSdkVersion="15" />
    <application android:name=".ui.HubroidApplication"
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher"
            android:theme="@style/Theme.Hubroid">
        <activity android:name=".ui.activities.Main"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
